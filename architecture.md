velox/
│
├── core/              → DB + index + search + launch
├── gui/               → glass UI
├── tui/               → terminal UI
├── cli/               → command tool
├── platform/windows/  → hotkey + launcher + icon extraction
└── main.v             → GUI entry